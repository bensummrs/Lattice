{
  "name": "lattice",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "glyph": "node render-glyph.js",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "vite": "^7.1.0"
  }
}
